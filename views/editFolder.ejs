<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <title> <%= title %> </title>
</head>
<body>
    <%- include('partials/navBar.ejs') %>
    <%- include('partials/sidebar.ejs') %>
    <section class="main-section">
        <div class="edit-folder-section">
            <form action="/folder/edit/<%= folder.id %>" method="POST" class="editFolder-form">
                <label for="updatedName">Folder Name:</label>
                <input type="text" name="updatedName" 
                    id="updatedName" value="<%= folder.name %>">

                <button>Confirm</button>
            </form>
            <!-- delete folder: -->
            <a href="/folder/delete/<%= folder.id %>">
                Delete
            </a>
        </div>
    </section>
    
</body>
</html>