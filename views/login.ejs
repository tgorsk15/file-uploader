<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/login-signup.css">
    <title><%= title %></title>
</head>
<body>
    <section class="login-section">
        <div class="login-err-container">
            <% if (locals.message && message.length > 0) {%>
                <h3 class="login-fail-message">
                    Login failed: <%= message %>
                </h3>
            <% } %>
        </div>
        <div class="login-container">
            <form action="/user/signIn" method="POST" class="login-form">
                <h2 class="login-form-header">Sign In</h2>
                <label for="username">Username:</label>
                <input type="text" name="username" id="username">

                <label for="password">Password:</label>
                <input type="text" name="password" id="password">

                <button>Log In</button>
            </form>
            <div class="signup-prompt-container">
                <h4>Don't have an account? Create one 
                    <a href="/user/signUp" class="signup-prompt-link">Here</a>
                </h4>
            </div>
        </div>
    </section>
</body>
</html>